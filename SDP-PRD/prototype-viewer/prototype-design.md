# SDP 原型阅读器 — 设计文档

> 版本：v1.0 | 日期：2026-05-29

---

## 一、目标与定位

将每个模块的 md 规格文档与对应的系统原型页面绑定，形成一个**可交互的原型阅读器**。

核心价值：
- 产品/开发阅读文档时，能直接看到对应的界面原型
- 交互到某个按钮/弹窗时，文档面板自动跳转到对应章节
- 版本迭代时，能直观看到哪些地方有改动

---

## 二、整体架构

### 2.1 文件结构

```
sdp-prd-output/
├── prototype/                    ← 原型阅读器（壳）
│   ├── index.html                ← 入口，三栏布局 + 阅读器逻辑
│   ├── viewer.js                 ← 阅读器核心：路由、文档联动、diff 标记
│   ├── viewer.css                ← 阅读器样式（三栏、拖拽、角标）
│   └── marked.min.js             ← md 渲染（本地副本，离线可用）
│
├── prototype/pages/              ← 系统页面（可独立使用）
│   ├── spot-goods/
│   │   ├── list.html             ← 现货管理-列表页
│   │   ├── create-spu.html       ← 创建SPU
│   │   ├── edit-skc.html         ← 编辑SKC（弹窗）
│   │   └── ...
│   ├── style-management/
│   ├── design-task/
│   └── ...
│
└── modules/                      ← 已有的 md 文档（不动）
    ├── spot-goods.md
    ├── style-management.md
    └── ...
```

### 2.2 两层解耦

| 层 | 文件 | 职责 | 可独立使用 |
|---|---|---|---|
| 系统页面层 | `prototype/pages/**/*.html` | 纯 HTML 原型，包含字段/按钮/弹窗，带 `data-doc-ref` 索引属性 | ✅ 直接双击打开 |
| 阅读器层 | `prototype/index.html` + `viewer.js` | 三栏布局、菜单导航、文档联动、diff 标记 | ✅ 独立维护 |

两层通过 `data-doc-ref` 属性通信，阅读器用 `<iframe>` 嵌入系统页面，监听页面内的交互事件。

---

## 三、布局规格

### 3.1 三栏结构

```
┌─────────────┬──────────────────────────────┬──────────────────────┐
│  左侧导航    │        系统页面（iframe）       │    文档面板（md）      │
│  200px 固定  │        flex: 1 自适应          │  默认 360px，可拖拽   │
│             │                              │  最小 240px          │
│  模块菜单    │  面包屑 + 页面内容             │  最大 600px          │
│  子页面列表  │                              │                      │
└─────────────┴──────────────────────────────┴──────────────────────┘
```

- 左侧导航：固定 200px，不可拖拽
- 中间页面：`flex: 1`，最小宽度 600px
- 右侧文档：默认 360px，左边缘可拖拽调整，支持完全折叠（折叠后显示 24px 的展开条）
- 整体高度：`100vh`，三栏均 `overflow: hidden`，内部各自滚动

### 3.2 顶部工具栏

```
┌─────────────────────────────────────────────────────────────────┐
│  SDP 原型  v2.6.0 ▼   [当前模块: 现货管理]   ● 变更标记 ON/OFF  │
└─────────────────────────────────────────────────────────────────┘
```

- 版本切换下拉：切换后整个原型重新加载对应版本的页面和文档
- 变更标记开关：全局控制 diff 角标的显示/隐藏

---

## 四、交互规格

### 4.1 菜单导航

左侧菜单结构：

```
▼ 现货管理
    列表页
    创建SPU
    编辑SKC（弹窗）
    复色（弹窗）
    核价复核
    上传图片（弹窗）
    修改库存（弹窗）
▶ 款式管理
▶ 开款任务
▶ 商品管理
▶ 店铺管理
▶ 通知中心
```

点击子页面条目：
1. 中间 iframe 加载对应 html 文件
2. 右侧文档面板加载对应模块的 md 文件，滚动到 `## 2. 页面概述` 章节
3. 面包屑更新为 `现货管理 / 列表页`

### 4.2 文档联动（核心）

系统页面内每个可交互元素带 `data-doc-ref` 属性，格式：

```
data-doc-ref="模块id#章节锚点"
```

示例：
```html
<!-- 列表页的"新增"按钮 -->
<button data-doc-ref="spot-goods#新增SPU">新增</button>

<!-- 筛选区域 -->
<div class="filter-bar" data-doc-ref="spot-goods#列表筛选条件">...</div>

<!-- 弹窗触发 -->
<button data-doc-ref="spot-goods#编辑SKC弹窗">编辑</button>
```

联动触发时机：
- **hover**：右侧文档面板高亮对应章节（黄色背景，300ms 过渡）
- **click**：文档面板滚动定位到对应章节，高亮持续 2s

阅读器通过 `iframe.contentDocument` 监听 `mouseover` 和 `click` 事件，读取最近祖先的 `data-doc-ref` 属性。

### 4.3 弹窗 / 抽屉交互

系统页面内的弹窗和抽屉直接用 HTML/CSS 实现（不依赖外部组件库）：

- 弹窗：`position: fixed` 遮罩 + 居中内容，点击遮罩关闭
- 抽屉：从右侧滑入，`transform: translateX`
- 弹窗/抽屉打开时，同步触发文档联动，文档面板跳转到对应章节

### 4.4 页面内跳转

部分操作会跳转到新页面（如点击"新增"跳转到创建SPU页）：

- 在原型中，这类跳转通过左侧菜单高亮切换 + iframe src 变更来模拟
- 系统页面内的跳转链接使用 `data-nav="页面id"` 属性，阅读器拦截并处理

---

## 五、变动标记规格

### 5.1 标记来源

md 文件中，变动内容通过以下方式标注（已有约定）：

```markdown
| 字段名 | 说明 | (v2.6.0 新增) |
```

或章节标题带版本标注：

```markdown
### 3.2 SKC 项目类型 (v2.6.0 变更)
```

阅读器解析 md 时，识别 `(v2.x.x 新增)` / `(v2.x.x 变更)` / `(v2.x.x 删除)` 标记，生成变动索引。

### 5.2 角标样式

```
字段名 [NEW]    ← 橙色角标，新增
字段名 [CHG]    ← 蓝色角标，变更  
字段名 [DEL]    ← 红色角标，删除（带删除线）
```

系统页面中，对应字段/按钮/区域带 `data-change="new|changed|deleted"` 属性：

```html
<label data-change="new">项目类型</label>
<button data-change="changed">批量操作</button>
```

变更标记开关 ON 时：
- 带 `data-change` 的元素右上角显示角标
- 点击角标，文档面板跳转到对应变更说明

### 5.3 变更汇总入口

顶部工具栏的版本号旁有「变更清单」按钮，点击后在文档面板展示当前版本所有变更条目，每条可点击跳转到对应页面+章节。

---

## 六、文档面板规格

### 6.1 渲染

- 使用 `marked.js` 将 md 渲染为 HTML
- 支持表格、代码块、mermaid 状态图（用 mermaid.js 渲染）
- 章节锚点：每个 `##` / `###` 标题自动生成 `id`，供联动定位使用

### 6.2 面板内导航

文档面板顶部有迷你目录（TOC），列出当前 md 的所有二级标题，点击快速跳转。

### 6.3 拖拽调整宽度

- 拖拽手柄：文档面板左边缘 4px 宽的竖条，hover 时变蓝
- 拖拽范围：240px ～ 600px
- 完全折叠：宽度拖到 < 240px 时自动折叠为 24px 展开条
- 宽度记忆：存入 `localStorage`，刷新后恢复

---

## 七、系统页面规格

### 7.1 设计原则

- **信息完整**：所有字段、按钮、状态标签必须出现，不能省略
- **布局还原**：参照 md 中的页面描述，还原真实的表单/列表布局
- **轻量实现**：纯 HTML + CSS，不引入 Vue/React，允许用少量原生 JS 处理交互
- **可独立打开**：双击 html 文件可直接在浏览器中查看，不依赖阅读器

### 7.2 组件约定

| 组件 | 实现方式 |
|---|---|
| 列表/表格 | `<table>` + 模拟数据（3-5行） |
| 筛选栏 | `<input>` + `<select>` 原生元素 |
| 按钮 | `<button>` + CSS 样式 |
| 弹窗 | `<div class="modal">` + JS 控制显示 |
| 抽屉 | `<div class="drawer">` + CSS transform |
| 表单 | `<form>` + 分组 `<fieldset>` |
| 标签/Badge | `<span class="badge">` |
| 面包屑 | `<nav class="breadcrumb">` |

### 7.3 模拟数据

每个列表页内置 3-5 条模拟数据，数据要有代表性（覆盖不同状态）。

---

## 八、开发顺序

1. **阅读器壳**：`prototype/index.html` + `viewer.js` + `viewer.css`
   - 三栏布局、菜单、iframe 加载、文档面板渲染
   - 拖拽调整宽度
   - 顶部工具栏（版本切换、变更标记开关）

2. **现货管理**（作为第一个完整模块验证联动机制）
   - `list.html`：列表页（最复杂，优先验证）
   - `create-spu.html`：创建SPU表单
   - `edit-skc.html`：编辑SKC弹窗
   - `upload-image.html`：上传图片弹窗

3. **联动机制验证**：确认 `data-doc-ref` 联动、变更角标、拖拽均正常

4. **其余模块**：按 v2.6.0 变更清单顺序逐模块补充

---

## 九、已确认事项

- [x] **版本切换**：支持，顶部下拉可在 v2.4.1 / v2.6.0 之间切换，切换后加载对应版本的页面和文档
- [x] **视觉风格**：通用 B 端简约风格，不参考真实系统截图
- [x] **模拟数据**：不需要与真实业务数据对齐，使用通用占位数据即可

## 十、GitHub Pages 发布约束

原型最终通过 GitHub Pages 发布，需满足以下约束：

**静态文件限制**：

- 所有资源必须是静态文件，不能有 Node.js / Python 等服务端逻辑
- `server.js` 不可用于生产，原型不依赖它

**md 文件加载**：

- GitHub Pages 支持通过 `fetch()` 加载同仓库的 `.md` 文件（相对路径）
- 文档面板改用 `fetch('../modules/spot-goods.md')` 加载，而非 `fs.readFile`
- 需要注意 CORS：同源请求无问题，本地双击打开时 `fetch` 会被浏览器拦截（需用本地服务器或 `file://` 协议的 workaround）

**本地开发**：

- 本地预览推荐用 `npx serve` 或 VS Code Live Server，避免 `file://` 协议的 fetch 限制
- 或提供一个 `open-local.bat` 脚本自动启动

**路径约定**：

- 所有内部链接使用相对路径，确保在 `https://user.github.io/repo/prototype/` 下正常工作
- 图片/字体等资源不引用外部 CDN（或明确标注需要网络）
- `marked.js` 和 `mermaid.js` 可用 CDN（jsDelivr），也可本地副本

**部署结构**：

```text
repo/
└── sdp-prd-output/
    └── prototype/
        ├── index.html          ← GitHub Pages 入口
        ├── viewer.js
        ├── viewer.css
        └── pages/
            └── spot-goods/
                └── list.html
```

GitHub Pages 配置：根目录或 `/docs` 分支均可，建议直接用 `main` 分支 + `/ (root)` 发布。
