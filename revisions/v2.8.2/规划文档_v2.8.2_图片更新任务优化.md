# 规划文档 · v2.8.2 图片更新任务优化

| 属性 | 内容 |
|------|------|
| 规划文档编号 | REV-20260727-001 |
| 关联版本 | v2.8.2 |
| 需求提出方 | 设计中心 |
| 规划人 | _待填写_ |
| 规划日期 | 2026-07-27 |
| 更新日期 | 2026-07-27 |
| 状态 | 规划中 |

---

## 1. 需求背景

### 1.1 问题描述

本版本集中解决图片更新任务模块的交互优化和数据完整性问题：

1. **视频/图片类型混淆**：当前任务类型为单选（图片或视频），但实际业务中需要同时更新SKC级别的图片和视频，且视频实际存储在款式管理的独立字段，与营销图分离。
2. **返修历史记录缺失**：多次返修后仅保留最新一次的返修说明，历史记录丢失，无法追溯完整的修改过程。
3. **上传操作不便**：更新内容区域无批量删除功能，只能逐个删除；修图说明仅支持单张参考图，限制了需求表达。
4. **查询能力不足**：列表页缺少 SKC 查询条件，只能通过 SPU 查询，定位具体颜色款式不便。
5. **美工协作信息缺失**：上传图片时无法记录美工备注，影响后续追溯和沟通。
6. **必填规则过严**：修图需求说明强制必填，部分场景（如简单的图片替换）无需详细说明，造成操作冗余。

### 1.2 需求清单

| 序号 | 需求描述 | 需求类型 | 模块 | 优先级 |
|------|---------|---------|------|--------|
| 1 | 视频/图片类型合并：支持同时勾选视频需求，视频为SKC维度，更新到款式管理的营销视频字段（与营销图分离），界面上分区展示 | 功能优化 | image-update | P0 |
| 2 | 保留多次返修的图片和说明记录：展示修图需求分类+图片快照+文本+处理人+时间 | 功能新增 | image-update | P1 |
| 3 | 上传图片弹窗，更新内容处增加全部删除按键 | 交互优化 | image-update | P2 |
| 4 | 列表增加 SKC 查询条件 | 功能优化 | image-update | P1 |
| 5 | 上传图片弹窗增加美工备注 | 功能新增 | image-update | P2 |
| 6 | 修图需求说明非必填 | 业务规则调整 | image-update | P2 |
| 7 | 修图需求说明支持上传多张图片 | 功能优化 | image-update | P1 |
| 8 | 营销图改为店铺维度多组：同一SKC可按店铺分别保存一组营销图；由运营发起任务并指定目标店铺（必填），美工执行上传，创建人（运营）审核；审核通过后图片写入该店铺维度的图组；保留"通用图"（无店铺绑定）兼容存量数据；**图片写入后写入操作日志，event_detail 补充 `target_store_id` / `target_store_name` 字段，标识本次更新的目标店铺维度** | 功能优化 | image-update + style-management | P0 |

### 1.3 版本说明

本版本基于 v2.7.1 迭代（主线版本），仅涉及图片更新任务 (image-update) 模块。

**版本依赖关系**：
- 如果 v2.8.0（加急标签功能）先于本版本上线，需要合并 v2.8.0 的加急功能到本版本模块文档
- 如果 v2.8.1（款式资料同步）涉及款式管理字段变更，需确认营销视频字段是否已在 v2.8.1 中定义

---

## 2. 影响分析

### 2.1 受影响模块

| 模块 | 当前版本 | 影响类型 | 影响说明 |
|------|---------|---------|---------|
| 图片更新任务 (image-update) | v2.7.1 | 直接 | 需求1-7：任务类型逻辑调整、返修历史记录新增、上传交互优化、查询条件扩展、字段新增、业务规则调整 |
| 款式管理 (style-management) | v2.8.1 | 间接 | 需求1：需要确认营销视频字段是否已定义，如未定义需同步新增 |
| 图片更新任务 (image-update) | v2.7.1 | 直接 | 需求8：任务新增目标店铺字段；发起方改为运营；审核逻辑调整为创建人审核；图片写入店铺维度 |
| 款式管理 (style-management) | v2.8.1 | 直接 | 需求8：新增 SKC 店铺维度营销图实体；SKC 详情页营销图展示调整为多组列表 |
| 商品管理 (product-management) | — | 间接 | 需求8：待上架选图弹窗新增店铺维度选择；商品图片更新通知增加店铺维度过滤；推送/复制商品时优先使用当前店铺维度图片 |

### 2.2 依赖传播链

```
图片更新任务 (image-update)
  │ 需求1：上传视频时更新款式管理营销视频字段
  ▼
款式管理 (style-management)
  └─ 需确认 SKC 实体是否已有 marketing_video 字段

图片更新任务 (image-update)
  │ 需求8：审核通过后图片写入 SKC 店铺维度营销图
  ▼
款式管理 (style-management)
  └─ 新增 skc_store_material 实体，关联 SKC + 店铺 + 营销图列表

款式管理 (style-management) — skc_store_material
  │ 需求8：商品管理读取 SKC 店铺维度营销图
  ▼
商品管理 (product-management)
  ├─ 待上架选图弹窗：新增店铺维度选择，按当前店铺过滤可选图组
  ├─ 图片更新通知：仅当通用图或对应店铺图组有更新时才触发通知
  └─ 推送/复制商品：优先取当前店铺维度营销图，无则降级到通用营销图
```

### 2.3 冲突分析

| 冲突点 | 涉及模块 | 说明 | 解决方案 |
|--------|---------|------|---------|
| 无 | — | 本版本功能均为独立优化，不修改现有核心流程 | — |

---

## 3. 模块变更详情

### 3.1 模块：图片更新任务 (image-update)

#### 变更概述

本次变更聚焦交互体验和数据完整性提升，核心变更包括：
1. 任务类型从单选改为支持同时处理图片+视频（视频为 SKC 维度，存储到款式管理独立字段）
2. 新增返修历史记录表，保留完整修改链路
3. 优化上传和创建页面交互细节
4. 扩展查询和数据记录能力

#### 页面变更

##### 需求1-7（图片/视频任务优化）

1. **图片更新任务列表** `修改` — 查询条件新增「SKC 编号」文本输入（支持批量，逗号分隔）；按 SKC 精确查询，任一匹配即返回任务（需求4）
2. **发起图片/视频更新** `修改` — 去掉更新内容类型（需求1）
3. **创建修图任务** `修改` — 
  a. 页面增加勾选 视频需求，勾选后出现视频需求说明；
  b. 修图需求说明改为非必填；说明参考图由单张改为最多5张（需求1/6/7）；
  c. 增加图片维度选择（需求8）——  
  顶部默认选中 通用，可批量切换至对应店铺；允许跨店铺组选择
  两种模式：
  **①新增**：如果当前款式没有对应店铺的图片，支持选择其他店铺的图片，修改后将结果保存到对应店铺的图组；
  **②编辑**：如果当前款式已经有图片，修改后将结果更新到对应的店铺

4. **上传图片/视频** `修改` — 分为图片上传区域和视频上传区域；图片区域更新内容新增「全部删除」按钮；新增「美工备注」输入框（长文本，最大200字，非必填）；视频区域支持上传1个视频（最大100MB，mp4/mov/avi）（需求1/3/5）
5. **审核更新结果** `修改` — 分区展示图片/视频审核区域；审核页右上角新增「查看返修记录 (n)」链接，展开折叠面板按时间倒序显示每次返修记录（返修批次/类型/说明/说明图片/提交内容快照/美工备注/处理人/时间）（需求1/2）

##### 需求8（营销图店铺维度分组）

1. **发起图片/视频更新** `修改` — 新增「目标店铺」单选下拉（可选，从任务关联 SPU 所属店铺中选择，注意设计时作为一个标签，后面可能会支持选店铺/风格/视觉类型等等）；选择后图片将写入该店铺维度的营销图组；不填则写入通用营销图（兼容存量）；一个任务只能绑定一个店铺
2. **创建修图任务** `修改` — 左侧营销图展示来源随目标店铺切换：有目标店铺时展示该店铺维度的营销图（skc_store_material），无目标店铺时展示通用营销图（prototype_material）；美工基于对应店铺的图填写修图需求
3. **上传图片/视频** `修改` — 审核通过后图片写入目标由「全局覆盖通用营销图」改为「写入任务绑定店铺维度的营销图组（skc_store_material 表）」；无目标店铺则仍写入通用营销图（原逻辑兼容）；不影响其他店铺的营销图
4. **审核更新结果** `修改` — 审核权限由「创建人或该SPU对应SKC设计师」调整为「仅创建人（运营）」；审核通过后图片写入任务绑定店铺维度的营销图组

---

#### 数据模型变更

| 实体 | 变更类型 | 变更内容 | 说明 |
|------|---------|---------|------|
| 图片更新任务 | 字段变更 | `task_type`：由枚举（图片/视频）改为 JSON 数组 | (v2.8.2 变更) 存储 ["image"] / ["video"] / ["image", "video"]，支持同时包含图片和视频 |
| 图片更新任务 | 新增字段 | `artist_notes`：长文本，可选，最大 200 字 | (v2.8.2 新增，需求5) 美工备注，上传图片/视频时填写 |
| 图片更新任务 | 新增字段 | `related_skcs`：JSON 数组，可选 | (v2.8.2 新增) 任务关联的 SKC 列表（从 SPU 展开），用于按 SKC 查询 |
| 图片更新任务 | 新增字段 | `target_store_id` / `target_store_name`：引用+短文本，可选 | (v2.8.2 新增，需求8) 任务目标店铺；审核通过后图片写入该店铺维度的营销图组；为空则写入通用营销图（兼容存量）|
| 修图需求明细 | 字段变更 | `requirement_images`：由单张图片引用改为 JSON 数组 | (v2.8.2 变更，需求7) 支持上传多张修图说明参考图，最多 5 张 |
| **返修历史记录**（v2.8.2 新增） | 新增实体 | 见下表 | (v2.8.2 新增，需求2) 记录每次返修的完整信息 |
| **SKC 店铺营销图**（v2.8.2 新增，需求8） | 新增实体 | 见下表 | (v2.8.2 新增，需求8) 存储 SKC 按店铺维度的营销图组；保留原 prototype_material 作为"通用图"兼容存量 |

**返修历史记录实体**（v2.8.2 新增）：

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| 记录ID | 整数 | 是 | 自动生成 | 主键 |
| 任务ID | 引用 | 是 | - | 关联图片更新任务 |
| 返修批次 | 整数 | 是 | - | 第几次返修（从 1 开始递增） |
| 返修类型 | 枚举 | 是 | - | IMAGE（图片返修）/ VIDEO（视频返修） |
| 返修说明 | 长文本 | 是 | - | 审核不通过时填写的返修说明，最大 500 字 |
| 返修说明图片 | 文件引用 | 否 | - | 审核不通过时上传的说明图片 |
| 提交时内容快照 | JSON 数组 | 是 | - | 该次提交时的更新内容图片/视频 URL 列表 |
| 美工备注 | 长文本 | 否 | - | 该次上传时填写的美工备注 |
| 处理人 | 短文本 | 是 | - | 该次上传操作人 |
| 处理时间 | 日期时间 | 是 | 当前时间 | 该次上传时间 |
| 创建时间 | 日期时间 | 是 | 当前时间 | 记录创建时间 |

**SKC 店铺营销图实体**（v2.8.2 新增，需求8）：

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| 记录ID | 整数 | 是 | 自动生成 | 主键 |
| SKC ID | 引用 | 是 | - | 关联 prototype 主键 |
| SKC 编码 | 短文本 | 是 | - | SKC 编码，冗余存储，最大 32 字符 |
| 店铺ID | 引用 | 是 | - | 关联店铺管理 |
| 店铺名称 | 短文本 | 是 | - | 店铺名称，冗余存储，最大 64 字符 |
| 营销图列表 | JSON 数组 | 是 | - | 该店铺维度的营销图 URL 列表（1-10 张） |
| 更新时间 | 日期时间 | 是 | 当前时间 | 最后更新时间 |
| 更新人 | 短文本 | 是 | - | 最后更新操作人 |
| 创建时间 | 日期时间 | 是 | 当前时间 | 记录创建时间 |

**存储规则**（v2.8.2 新增，需求8）：
- 一个 SKC + 一个店铺 = 一条记录
- 同一 SKC 可有多条记录（不同店铺）
- 原 `prototype_material` 表保留，存储"通用营销图"（无店铺绑定），兼容存量数据
- 新任务如未指定目标店铺（为空），仍写入 `prototype_material`

---

#### 依赖变更

| 变更类型 | 目标系统 | 类型 | 说明 |
|---------|---------|------|------|
| 新增依赖 | 款式管理 (style-management) | hard | (v2.8.2 新增) 需要确认 SKC 实体是否已有 `marketing_video` 字段，用于存储营销视频；如未定义需同步新增 |

---

#### 业务规则变更汇总

| 变更点 | 原规则 | 新规则（v2.8.2） |
|--------|--------|---------|
| 任务类型 | 单选（图片或视频） | 多选（支持同时勾选图片+视频） |
| 修图说明必填 | 至少需要输入一个修图说明 | 修图说明改为非必填（需求6） |
| 修图说明参考图 | 每个输入框可输入一张图 | 每个说明最多上传 5 张参考图（需求7） |
| 视频大小限制 | 最大 50MB | 最大 100MB |
| 返修记录 | 仅保留最新一次 | 保留完整历史记录（需求2） |
| 更新内容删除 | 逐个删除 | 新增「全部删除」按钮（需求3） |
| 美工协作 | 无记录字段 | 新增美工备注字段（需求5） |
| SKC 查询 | 不支持 | 列表新增 SKC 查询条件（需求4） |
| 营销图存储维度（v2.8.2 新增，需求8） | SKC 全局一组通用营销图 | SKC × 店铺各一组；保留通用营销图兼容存量 |
| 任务发起方（v2.8.2 新增，需求8） | 设计师 | 运营（通过指定目标店铺发起） |
| 审核权限（v2.8.2 变更，需求8） | 创建人或 SPU 对应 SKC 设计师 | 仅创建人（运营）可审核 |
| 图片写入目标（v2.8.2 变更，需求8） | 覆盖写 SKC 通用营销图 | 写入任务绑定店铺维度的营销图组；不影响其他店铺 |
| 操作日志（v2.8.2 变更，需求8） | 日志不含店铺维度 | 写入操作时 `event_detail` 补充 `target_store_id` / `target_store_name`，标识本次更新的目标店铺；通用图写入时两字段为空 |

---

### 3.2 模块：款式管理 (style-management)

#### 变更概述（需求8直接影响）

**(v2.8.2 新增，需求8)** 为支持 SKC 营销图按店铺维度分组存储，需新增 `skc_store_material` 实体。

#### 数据模型变更

新增实体 `skc_store_material`（SKC 店铺营销图，v2.8.2 新增）：

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | 整数 | 自动 | 主键（雪花ID） |
| prototype_id | 引用 | 是 | 关联 prototype 主键 |
| design_code | 短文本 | 是 | SKC 编码，冗余存储，≤32字符 |
| store_id | 引用 | 是 | 关联店铺管理 |
| store_name | 短文本 | 是 | 店铺名称，冗余存储，≤64字符 |
| marketing_images | JSON数组 | 是 | 该店铺维度的营销图 URL 列表（1-10 张） |
| updated_at | 日期时间 | 自动 | 最后更新时间 |
| updated_by | 短文本 | 是 | 最后更新操作人，≤64字符 |
| created_at | 日期时间 | 自动 | 记录创建时间 |

唯一索引：`(prototype_id, store_id)`，同一 SKC 同一店铺只保留一条记录。

#### 页面变更

**SKC 详情页 — 营销图区域（v2.8.2 变更，需求8）**

| 区域 | 原展示方式 | 新展示方式 |
|------|---------|---------|
| 营销图 | 单组图片直接展示 | 列表形式展示多组：每行一个店铺 + 对应图组；第一行始终为「通用营销图」（无店铺） |

**展示规则**：
- 「通用营销图」始终展示在第一行，来源：`prototype_material` 表（存量兼容）
- 店铺维度营销图从 `skc_store_material` 表查询，按店铺名称排序
- 每行支持独立编辑（上传/删除），编辑权限与原营销图一致
- 无数据的店铺行不展示（按需加载）

---

### 3.3 模块：商品管理 (product-management)

#### 变更概述（需求8间接影响）

**(v2.8.2 新增，需求8)** 营销图升级为店铺维度多组后，商品管理在选图、通知、推送/复制三个场景均需感知店铺维度，确保商品侧始终使用正确店铺的图片，避免图片错配。

#### 页面变更

1. **待上架 — 选择图片弹窗** `修改` — 新增「店铺」维度选择（单选，从商品所属店铺中取值）；选择店铺后，可选图组优先展示该店铺维度的营销图（`skc_store_material`），同时保留「通用」选项展示通用营销图（`prototype_material`）

2. **商品图片更新通知** `修改` — 通知触发条件收窄：仅当「通用营销图」或「与当前商品所属店铺匹配的店铺维度图组」有更新时，才向对应商品发送通知；其他店铺维度图组的更新不触发通知，避免无关推送

3. **推送待上架 / 复制商品** `修改` — 图片取用逻辑调整为：**优先取当前店铺维度营销图**（查询 `skc_store_material`，`store_id` = 当前商品所属店铺），若该店铺维度无数据或图组为空，则降级使用通用营销图（`prototype_material`）

#### 商品管理业务规则变更

| 变更点 | 原规则 | 新规则（v2.8.2） |
| ------ | ------ | --------------- |
| 待上架选图来源 | 仅展示通用营销图 | 优先展示当前店铺维度营销图；保留通用图选项 |
| 图片更新通知范围 | 任意图组更新均通知 | 仅通用图或匹配店铺的图组更新才通知 |
| 推送/复制商品图片取用 | 固定取通用营销图 | 优先取当前店铺维度图；为空时降级取通用图 |

---

## 4. 不在本期范围内

- 视频自动转码和多清晰度支持
- 返修历史记录的导出功能
- 批量审核功能
- 修图说明的模板化配置
- 美工备注的结构化字段（如美工姓名、处理时长等）
- SKC 查询的模糊匹配和关联搜索
- 店铺维度营销图的批量复制功能（v2.8.2 需求8）
- 店铺维度营销图的版本历史追溯（v2.8.2 需求8）

---

## 5. 待确认事项

- [ ] 款式管理 SKC 实体是否已有 `marketing_video` 字段（需与款式管理模块负责人确认）
- [ ] 视频存储服务的容量和带宽是否支持 100MB 视频上传
- [ ] 返修历史记录表的数据保留策略（是否需要定期归档）
- [ ] v2.8.0（加急功能）是否已上线，如未上线需合并到本版本文档
- [ ] （v2.8.2 需求8）店铺维度营销图的命名规则是否需要在图片文件名中体现店铺信息
- [ ] （v2.8.2 需求8）存量数据是否需要迁移到"默认店铺"维度，还是保持通用图即可

---

## 6. 执行记录

| 日期 | 操作 | 操作人 | 结果 |
|------|------|--------|------|
| 2026-07-27 | 创建规划文档 | _待填写_ | — |

---

## 7. 变更汇总

> **viewer 数据源**：此表格是 prd-viewer 判断模块变更类型的唯一权威来源。

| 模块 | 变更前版本 | 变更后版本 | 变更类型 | 状态 |
|------|----------|----------|---------|------|
| 图片更新任务 (image-update) | v2.7.1 | v2.8.2 | 功能优化 | pending |
| 款式管理 (style-management) | v2.8.1 | v2.8.2 | 实体新增 + 页面调整 | pending |

---

## 附：相关文档索引

| 文档 | 路径 | 说明 |
|------|------|------|
| 通用设计语言规范 | references/通用设计语言规范.md | 字段类型、页面类型标准 |
| 文档结构模板 | references/文档结构.md | 模块文档结构模板 |
| 前序版本 | revisions/v2.7.1/规划文档_v2.7.1_价格追踪与PLM审版联动.md | v2.7.1 规划文档 |
| 并行版本 | revisions/v2.8.0/规划文档_v2.8.0_开款流程优化.md | v2.8.0 规划文档（可能包含加急功能） |
| 并行版本 | revisions/v2.8.1/规划文档_v2.8.1_款式资料同步.md | v2.8.1 规划文档（需确认营销视频字段） |
